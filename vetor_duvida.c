#include<stdio.h>
#include<stdlib.h>

#define MAX 5
int main()
{
  int maior;
  int vetor[MAX];
  //ter o vetor
  for (int i = 0; i<MAX; i++)
    {printf("Digite o valor %d: ",i);
      scanf("%d",&vetor[i]);
    }
  maior = vetor[0];
  for (int i = 0; i<MAX; i++)
{
}
}
