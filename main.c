#include <stdio.h>
int main (void)
{
	float calcularAreaRetangulo(float x,float y);//declaração da variável
	float areacalcularAreaRetangulo(10.0,20,0);// copia dos valores que estão na var retangulo..
	printf("A area eh:%f",area);
	return 0;
}
float calcularAreaRetangulo(float x, float y) {
//calcular area quadrado
float area=base * altura;
return area;
}
