#include <stdio.h>
int main(void) 
	{
int i,n;
double t = 1, s = 1,x;
printf("Insira n: ");
scanf("%d", &n);
  for (i = 1; i <= n; i++)
    {
      t *= x;
      t /= i;
      printf ("o 1º valor de i é: %d\v",i);
      s += t;
    }
}
