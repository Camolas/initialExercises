#include <stdio.h>
int main ()
	{
		int data;
		printf("Boa noite, escreva uma data pelo formato/ordem dd mm aaaa")
		scanf("%d",&data);
			{
				case '366': puts('é bissexto');// ou uma string (extenso-trezentos e...)
				case '365': puts('ano comum');
				default: puts('parametros não validados');
			}
	}

