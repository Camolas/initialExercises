#include <stdio.h>
#define DIM 3
#define ESPACO ' '
/*
void Gerar (int *v, int n)
	{
		int i,indice;
		for (i=1;
	}
*/
void inic(char s[][DIM]) /* Omitir uma dimensao */
{
	int i,j;
	for (i=0;i<DIM;j++)
		printf("%c %c",s[i] [j], j== DIM-1? 
}

int main (void)
{
	int i;
	const int max=9;
	int vet[7]={32,92,0,12}; // 'a' says: 

    for (i=0;i<(max-1);i++) // int i inside loop, it says: note: use option -std=c99 or -std=gnu99 to compile your code
    {
		printf("%d\n",(vet[i]));
	}
    return 0;
}

