#include <stdio.h>
int n;
int fn (int n)
{ 
	if (n>0)
	for (soma=0;n>0;n--)
	{
		soma+=1/n
	}
	return soma; 
	return -n;
	/*else
	 *  n=-n
	 * return n*/
int main
{
	int cont;//n lê-a se for variável do main?
	while(fn>5 && //as 2 condições tem de ser satisfeitas
	scanf("%d",&n); 
	
	
