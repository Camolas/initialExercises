#include <stdio.h>
#include <math.h>
int main(void)
  {
	float x,soma;  
	int n=5,exp;/*variavéis locais*/
	
	scanf("%f%d",&x,&n);
	printf("%f%f\n",expx(x,n),exp(x)); 
   

}
float sin(float x,int x){
	  float p,s;
	  int i=1,f=1;
	  s=1;
	for (i=1;i<n;i++)
	{
	p*=x*x;//numerador 
	f=*(2*i+1);//denominador
	u *= -1
	s=s+(u*p)/f;
    }

float cos(float x,intx){

	  float p,s;
	  int i,f=1;
	  s=1;
	for (i=x;i<n;i++)
	{
	p*=x*x;
	f=(f*2*i);
	u *= -1
