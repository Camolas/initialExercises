#include <stdio.h>
main ()
{				//warning's
   int dia, mes, ano;
  printf ("Escreva o dia do seu aniversário\n");
  scanf ("%d", &dia);
  printf ("o mês\n"); scanf ("%d", &mes);
  printf ("e no te esqueças do ano");
  scanf ("%d", &ano);
  printf ("a data é:3 % d ", dia, mes, ano);/*cena dos 3 % d */
    return 0;
}
