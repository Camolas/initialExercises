#include <stdio.h>
int main()
	{
		int n=0,k,seq[n],sum,consultas;
		do
		{
		scanf("%d",&seq[n]);
		n++;
		if (n>100) seq[n]=0;
		}
		while(int consultas;consultas==0,--consultas)
		scanf("%d",&consultas);
		scanf("%d",&k);
		for (k;
	}

/* ----LEGENDA DO PROGRAMA----
   k<-indica um inteiro que vai servir de mote para encontrar os seus multíplos na seq.
   n<-index da sequencia de int
   seq[]<-vetor que armazena valores-que procura multiplos dos positivos k
   sum<-variável que calcula a soma dos multiplos  de k
