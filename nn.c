#include<stdio.h>
main(){
  int x = 99, y = '\120';
  printf("%c tem o codigo ASCII %d\n",x,x);
  printf("O codigo ASCII %d (octal %o)  corresponde a %c\n",y,y,y);
  printf ("o 0 O codigo ASCII %d (octal %o) corresponde a %c\n",x,x,x);
    printf(" o código ASCII %d(hexa %x) corresponde a %c\n",y,x,y);
    printf(" enganei-me %x é o hexa do numero acima\n",y);
}

