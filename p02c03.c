#include <stdio.h>
main ()
{
	int x, y=3,z=78;
	puts("escreva o valor de x\n"); scanf("%d",&x);
	if (x>0)
		printf("%d é positivo\n",x);
	else 
		if (y>0 & y<=77)
			printf("%d está comprendido entre à esquerda por x e menor que z\n",y);
}
			
