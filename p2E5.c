#include <stdio.h>
#include <stdlib.h>
int main ()
{
	int v[50],max,i=1,iguais;
	max=v[2]-v[1]
	//abs
	while(++i<50)
	{
		if (v[i+1]-v[i]>max)
		max=abs(v[i+1]-v[i]);
		do
		{ 
			++iguais=
		
		}
		while (v[i+1]!=v[i])
	}
}
