#include stdio.h
int vec[];
int n=10,i;
//puts("digite um numero de vaçlores da sue"); scanf("%d",&n);
for (i=0;i<10;i++)
	{
		scanf("%d",&vec[i]);
		if (a[j] > a[j+1]) 
		{
				
            Aux = a[j];
            a[j] = a[j+1];
            a[j+1] = Aux;
            printf("a ordem 
		}


 For j=1 to i-1 do
         
